# Checkpoint — 2026-08-31 21:32 [none]

## Session summary
Long-running session on `cc-arch-hands`'s persistent agent-tree engine (`templates/skills/agent-new/assets/agent-tree.js` + `backends/claude.js`), now in its ninth consecutive external-review round this session. Pattern each round: user pastes or points at a review file → I verify every claim by reading the actual code and, wherever practical, reproducing it empirically in a throwaway stub-backend sandbox (real `claude` CLI calls are forbidden by default — I accidentally made two unauthorized real paid calls this session, ~$0.11 and ~$0.05, both immediately disclosed and the offending sandbox directories deleted, both times caused by copying `assets/` fresh without remembering to swap in the stub `backends/claude.js` first) → report a precise verdict per finding (confirmed / confirmed-but-lower-severity / already-known-and-deferred / refuted) → only fix when the user explicitly says so.

Round 7 (8 findings, all fixed and committed as `3783094`/`3055af6`... actually the numbering: round-7 fixes landed as commit `080f0e8`; a *second* pass of findings from the same round-7 conversation, discovered while re-verifying, landed as `3055af6`) covered: atomic fanout/budget reservation via a `.reserving` marker + parent-cell mutex, empty `--task` no longer billing, backend response-shape validation, `claude.js` argv quoting (a real command-injection PoC was reproduced and fixed — always-quote instead of regex-conditional quoting), and task-file mutex protection.

Round 8 (8 more findings) was implemented via **two parallel `/rush` sessions in isolated `git worktree`s** (`cah-round8-engine`, `cah-round8-docs`) per explicit user request ("реализуй исправления с помощью /rush & worktrees") — each got a long, fully-prescriptive prompt (exact solutions specified, not open design), both forbidden from committing/pushing/real-calls. After both completed, I independently re-verified every fix myself (diff review + my own sandbox repros, including a negative control against the pre-fix commit to prove the timing numbers were real: 60.8s → 0.7s for the headline die()-inside-mutex fix) before merging (`git merge` both branches into `main`, fast-forward + merge commit `6a1c7f7`, `npm test` 237/237) and cleaning up both worktrees + their branches per explicit request. Round-8 fixes: owner-token + `process.on('exit')` safety net extended from the budget-mutex/reservation (round-7 primitives) to session locks (`acquireLock`/`releaseLock`) and `_cell.mutex`/`_root.mutex` (`withMutexAt`), via a shared `releaseTokenChecked` helper; `wake()` re-reads session state fresh after acquiring the session lock (closes a stale-snapshot race with concurrent `session close`); `session new --from` refuses a closed source; failed `session new` now records `orphan_unknown_cost_calls` at the passport root (checked by `subtreeHasUnknownCost`); `task done` is now crash-consistent for the report-append step and `writeTaskBrief` also checks `tasks/done/` for same-minute slug collisions; `parseNumberArg`/`depth_max`/`fanout_max`/`budget_usd` reject Infinity/negative/fractional; new `assertSafeSegment()` reserves `parent`, forbids `()`  and most Windows-reserved names. Docs worktree corrected `budget_usd` (soft admission threshold, not a hard ceiling) and `permissionModel` (declarative label, engine only validates the enum, builds no enforcement from it) wording.

Round 9 (just verified, NOT yet fixed — this is where the session currently stands) found a **new, more severe class of bug** I hadn't previously considered: `assertSafeSegment` doesn't forbid JS-special property names (`__proto__`, `constructor`, `toString`, `valueOf`) and `meta.sessions`/similar are plain JS objects used as maps — `sessions['__proto__']` returns the inherited `Object.prototype` (truthy, so `if (!sess)` existence checks don't catch it). **Reproduced live**: `send a --session __proto__` against a stub backend returned exit 0, genuinely invoked the backend, billed a call — with **zero trace anywhere** (not in `ledger`, not even in `orphan_unknown_cost_calls`) — worse than any previously-found cost-invisibility gap, since even the "known gap" cases at least get a counter bump. Round 9 also confirmed: `session close` and `retire` both still race with `send` (their own state-flip isn't protected by the session lock, only checked cold beforehand — the round-8 fresh-recheck in `wake()` only re-verifies session-level `closed`, never node-level `retired`); `session new --from` is still only protected against an *already-visible* race (no lock held on the source through the fork call); `contacts add`'s duplicate-alias check races the same way session-close's old invariant check used to; `limits approve`/`request-limit` never got the round-8 numeric-validation hardening (still accept negative/fractional depth_max/fanout_max, negative budget_usd); Windows device-name blocking is exact-match only (`COM1.md` slips through); the crash-consistency `## Report (` marker in `task done` is a naive substring match that a user's own brief text could false-positive against; `orphan_unknown_cost_calls` isn't surfaced in `cost`/`tree` (only gates the budget check, invisibly); and several doc-internal contradictions (`agent/SKILL.md:35` still says exit 3 for a missing session, contradicting `:83` in the same file which correctly says exit 8; `:78`'s failed-turn wording still doesn't mention the budget-blocking consequence added in round 8). The read-then-unlink TOCTOU in the new `releaseTokenChecked` helper is real by code reading but I assessed it as practically negligible (microsecond window, no I/O boundary between the two syscalls) relative to the other findings, which have much wider (seconds-to-minutes) windows.

Also this session: added a new project skill, `/ccheckpoint` (`templates/skills/ccheckpoint/SKILL.md`, registered in `lib/manifest.js`'s `AllSkills`, README.md updated in every place skill counts/lists appear — table row, "Session memory in detail" prose, install-example line, directory-tree diagram, three separate "(10)"→"(11)" count fixes) — behaviorally identical to `/checkpoint` except it invokes `checkpoint` via the `Skill` tool (not a duplicated re-implementation, to stay in sync automatically) and then commits the resulting file (that one file only, never `-A`, never a push). Verified via `npm test` (237/237, unaffected since skill counts aren't tested per-name) and a real sandboxed install/uninstall round-trip through `cah`'s own CLI before installing it globally (`node bin/cah.js install --templates ./templates --only ccheckpoint`, byte-diffed against the template — differs only in the trailing sentinel, as expected). This very checkpoint is being written via that new skill, which is why this file is about to get committed immediately after being written, unlike every prior checkpoint this session.

## Active goal
none

## TaskList
Empty — `TaskList` returned no entries. This whole session's work was tracked conversationally (per-round: review → verify → report → fix-on-request), never via the TaskList tool.

## Decisions
- Round 9's `__proto__`/`constructor` finding, `session close`/`retire` TOCTOU with `send`, and `session new --from`'s remaining TOCTOU are all independently verified as real and not yet fixed — user has only asked me to study (not yet fix) round 9 as of this checkpoint.
- `releaseTokenChecked`'s read-then-unlink non-atomicity is real by code reading but judged practically negligible (no I/O wait between the two syscalls, unlike the multi-second-to-multi-minute windows in the other round-9 findings) — flagged as lowest priority of the round, not dismissed outright.
- The failed-birth (`cmdNew`) budget-invisibility gap remains explicitly, repeatedly deferred (rounds 7, 8, and re-confirmed in 9) as needing a genuine durable orphan-cost ledger, not a point patch — consistent decision maintained across three rounds.
- `/ccheckpoint` is a thin wrapper that invokes `/checkpoint` via the `Skill` tool rather than duplicating its behavior spec, specifically so the two skills can never drift out of sync with each other.
- Round 8's fixes were delegated to `/rush` in two parallel git worktrees (engine code vs. docs, chosen to have zero file overlap) rather than one shared worktree, per `/rush`'s own guidance against concurrent writers on the same files — and every fix was independently re-verified (including a negative-control repro against the pre-fix commit) before merging, never trusting the delegated session's own report.

## Open questions
- Round 9's full fix list (7 P1s + 8 P2s + 5 doc items) is verified but not yet actioned — waiting on the user to say "исправим"/give scope, per this session's established rhythm. The most severe (by far): the `__proto__` cost-invisible-call exploit.

## Repo state
```
 M README.md
 M lib/manifest.js
?? docs/agent-tree-review-2026-08-31-round-7.md
?? docs/agent-tree-review-2026-08-31-round-8.md
?? docs/agent-tree-review-2026-08-31-round-9.md
?? docs/agent-tree-review-2026-08-31.md
?? templates/skills/ccheckpoint/
?? tmp/
```
```
6a1c7f7 Merge branch 'fix/round8-docs'
e2bf204 docs(agent-tree): budget_usd and permissionModel wording corrected
e863b00 fix(agent-tree): owner-token everywhere, session races, task crash-safety
080f0e8 fix(agent-tree): owner-checked locks, session-lifecycle races, argv escaping
3055af6 fix(agent-tree): close resource leaks, unsafe shell quoting, task-file race
```
The `README.md`/`lib/manifest.js` modifications above are the uncommitted `/ccheckpoint` skill registration (skill file itself is untracked, not yet added) — not yet committed as of this checkpoint being written. The four `docs/agent-tree-review-*.md` files are external review artifacts, never authored or committed by me, left untracked throughout.
